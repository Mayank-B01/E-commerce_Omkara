import React from 'react';
import Layout from "../components/Layout/Layout.jsx";

const Policy = ({ handleShowAuthModal }) => {
    return(
        <Layout title={'Privacy Policy - Omkara'} handleShowAuthModal={handleShowAuthModal}>
            <h1>Policy</h1>
        </Layout>
    );
};
export default Policy;